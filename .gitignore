# Xcode project — always regenerated via `xcodegen generate`, never committed
*.xcodeproj
*.xcworkspace

# Build products
DerivedData/
.build/
*.ipa

# Secrets
*.p8
.env*
fastlane/report.xml
fastlane/README.md

# No Gemfile.lock is committed — see Gemfile for why.
Gemfile.lock

# macOS
.DS_Store

# Supabase CLI machine-local state (linked project ref, caches)
supabase/.temp/

# Agent worktrees and session state
.claude/

# Live smoke-run outputs (real model calls, regenerated on demand)
supabase/functions/tidynote_organize/smoke/out/
__pycache__/
